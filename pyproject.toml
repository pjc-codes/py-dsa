[project]
name = "py-dsa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.1.0",
]

[tool.uv.workspace]
members = [
    "random",
    "arrays",
]
